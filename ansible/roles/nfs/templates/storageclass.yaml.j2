---

apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: managed-nfs-storage
provisioner: nfs-provisioner
parameters:
  {{ nfs_provisioner_storageclass_parameters | to_nice_yaml(indent=2) }}
